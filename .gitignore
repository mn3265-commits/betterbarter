# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TypeScript build info
*.tsbuildinfo

# local env (never commit real keys)
.env
.env.*
!.env.example

# Contact details that belong on a form, not in a public repo.
pitch/contact.local.md

# Seeded demo rows — ids to clean up, not for the repo.
pitch/demo-data.local.md

# Cached webfont for the logo build, fetched on demand.
scripts/.fonts/
